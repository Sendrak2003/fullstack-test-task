import Typography from "@mui/material/Typography";

export function Header() {
  return (
    <header>
      <Typography variant="h1" component="h2">Список элементов</Typography>
    </header>
  );
}
